[[edge_functions]]
  function = "posthog-proxy"
  path = "/ph/*"

[functions]
  directory = "netlify/functions"

# /api/booking/* redirects live in public/_redirects, not here — netlify.toml
# redirects were not taking priority over the SPA catch-all on production.
